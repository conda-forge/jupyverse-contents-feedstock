schema_version: 1

context:
  version: "0.1.3"

package:
  name: jupyverse-contents
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/j/jupyverse-contents/jupyverse_contents-${{ version }}.tar.gz
    sha256: 77ddeb11215535c9c4ea528181bd856eb7091e12748f1d680535b9c016357915

build:
  noarch: python
  number: 0
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - uv-build
  run:
    - python >=${{ python_min }}
    - anyio
    - anyioutils
    - fastapi-core
    - jupyverse-api >=0.15.0,<0.16.0
    - jupyverse-auth >=0.2.0,<0.3.0
    - pydantic

tests:
  - python:
      imports: jupyverse_contents
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  license: BSD-3-Clause
  license_file: COPYING.md
  homepage: https://github.com/jupyter-server/jupyverse/tree/main/api/contents
  summary: The API for Jupyverse's contents.

extra:
  recipe-maintainers:
    - davidbrochart
